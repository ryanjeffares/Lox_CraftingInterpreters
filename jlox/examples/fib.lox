fun fib(n) {
    if (n < 2) {
        return n;
    }
    else {
        return fib(n - 2) + fib(n - 1);
    }
}

var start = clockMillis();
print fib(40);
var end = clockMillis();

print end - start;